<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css"
      integrity="undefined"
      crossorigin="anonymous"
    />
    {{! <style>
      @import url("https://fonts.googleapis.com/css?family=Quicksand:300,400");
      * { box-sizing: border-box; padding: 0; margin: 0; font-family:
      "Quicksand", sans-serif; } body { background-image:
      url("https://preview.ibb.co/daZ4GQ/1_Tk_Eo_B7y.jpg"); background-size:
      100vw 100vh; background-repeat: no-repeat; } section article { width: 80%;
      margin: 20px auto; display: flex; flex-direction: column; align-items:
      center; justify-content: center; height: 90vh; border-radius: 5px;
      box-shadow: 3px 3px 8px 0px #0e0e0e; background-color: rgba(255, 255, 255,
      0.12); } h2 { color: #fff; font-size: 70px; font-weight: 100; text-align:
      center; } span.orange { display: inline; font-size: 75px; color: #ffa700;
      margin-right: -15px; } p { font-size: 16px; padding: 10px; } form { width:
      500px; } input, textarea { width: 100%; border: 1px solid rgba(255, 255,
      255, 0.45); background-color: rgba(255, 255, 255, 0.28); padding: 10px;
      border-radius: 3px; } input::placeholder, textarea::placeholder { color:
      #fff; font-size: 18px; } label { display: block; color: #fff; font-weight:
      bold; padding: 10px; } /* button { width: 30%; display: flex; color:
      rgb(231, 223, 223); background-color: rgb(114, 81, 190); padding: 10px
      20px; font-size: 25px; margin: 20px auto; border: none; border-radius:
      5px; cursor: pointer; justify-content: center; } */ button { margin: 20px
      auto; background: #c58104; color: #fff; border: none; border-radius: 5px;
      position: relative; height: 50px; font-size: 1.2em; padding: 0 1em;
      cursor: pointer; transition: 800ms ease all; outline: none; } button:hover
      { background: #fff; color: #ffa700; } button:before, button:after {
      content: ""; position: absolute; top: 0; right: 0; height: 2px; width: 0;
      background: #ffa700; transition: 400ms ease all; } button:after { right:
      inherit; top: inherit; left: 0; bottom: 0; } button:hover:before,
      button:hover:after { width: 100%; transition: 800ms ease all; }
    </style> }}
  </head>
  <body>
    {{{body}}}

  </body>
</html>